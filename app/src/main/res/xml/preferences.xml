<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    
    <Preference android:title="@string/people">
        <intent android:action=".activities.PeopleActivity" />
    </Preference>
    
    <Preference android:title="@string/rooms">
        <intent android:action=".activities.RoomsActivity" />
    </Preference>
    
    <Preference android:title="@string/add_esensor">
        <intent android:action=".activities.AddESensorActivity" />
    </Preference>
    
    <Preference android:title="@string/add_fitbit">
        <intent android:action=".activities.AddFitbitActivity" />
    </Preference>
    
</PreferenceScreen>